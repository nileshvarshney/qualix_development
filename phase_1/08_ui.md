You are working inside an existing application that already has functioning pages, layouts, routes, navigation, and components.

Your task is to ADD the missing Phase 1 UI screens for the Data Quality platform WITHOUT damaging, replacing, restructuring, or refactoring the existing working pages.

This is a safe additive change only.

## Goal

The app already shows some existing pages, plus Connector and Asset Registry pages.

I need you to introduce ONLY the missing Phase 1 screens:
- Operations > Scan Jobs
- Operations > Run History
- Operations > Run Detail / Logs
- Administration > Users
- Administration > Teams
- Administration > Roles

These new pages must appear in the UI, but existing pages must remain unchanged and fully functional.

## Critical safety rules

1. Do NOT rewrite the app shell.
2. Do NOT replace existing routes.
3. Do NOT rename or move existing pages unless absolutely required.
4. Do NOT refactor unrelated components.
5. Do NOT change existing navigation labels or hierarchy unless needed to insert the new items.
6. Do NOT break existing pages, route guards, layouts, theming, or API integrations.
7. Reuse existing patterns, layout wrappers, table components, page containers, and design system components wherever possible.
8. If an existing component already solves part of the problem, reuse it instead of creating a competing version.
9. Prefer additive code changes over destructive changes.
10. If a backend API already exists, use it. Only add missing API endpoints if absolutely necessary.
11. Keep all existing pages reachable exactly as they are today.
12. Do not make broad architectural changes.

## Required workflow

Follow this sequence exactly:

### Step 1: Audit first
Inspect and list:
- current route definitions
- current sidebar or navigation configuration
- current page/layout structure
- current role/permission guards
- current API client functions related to jobs, runs, users, teams, roles
- which Phase 1 backend endpoints already exist
- which UI screens already exist
- which new screens are missing

Do not code yet.
First produce an audit report.

### Step 2: Minimal change plan
Based on the audit, propose the smallest safe set of changes needed to introduce the missing screens.

Your plan must explicitly state:
- which files will be modified
- which new files will be created
- which existing files will NOT be touched
- how you will avoid regressions

Do not code yet until the plan is clear.

### Step 3: Implement only additive UI changes
Add the missing screens using the existing app structure.

Required new UI sections:
- Operations
  - Scan Jobs
  - Run History
  - Run Detail / Logs
- Administration
  - Users
  - Teams
  - Roles

Implementation requirements:
- integrate these pages into the existing sidebar/navigation without disturbing existing items
- use existing layout/page shell
- use existing styling system and component conventions
- if the app already has tabs instead of sidebar groups, adapt to the current pattern rather than inventing a new one
- preserve all existing routes and flows
- preserve current role visibility behavior and extend it carefully for new screens
- add loading, empty, and error states
- if a page has no data yet, still render a usable empty state rather than hiding the page

### Step 4: Verify existing pages still work
After implementation:
- verify existing routes still resolve
- verify existing pages still render
- verify new routes render
- verify navigation includes new items
- verify no existing navigation items disappeared
- verify role guards still behave correctly
- verify no major layout regressions were introduced

## Output format

Return your response in this exact structure:

1. Audit Findings
2. Existing Files to Reuse
3. Missing Screens
4. Minimal Safe Change Plan
5. Files to Create
6. Files to Modify
7. Files Explicitly Not Touched
8. Code Changes
9. Regression Checklist
10. Final Screens Now Visible

## Additional constraints

- Prefer appending new route entries instead of rewriting route config
- Prefer appending new nav items instead of rebuilding the sidebar
- Do not migrate routing systems
- Do not replace table or form libraries
- Do not restyle the application globally
- Do not change data contracts unless required
- If any backend endpoint is missing, add the smallest possible endpoint and clearly isolate that change
- If a route already exists for similar admin pages, extend that pattern
- If there are feature flags or role guards hiding the new pages, expose them carefully without changing unrelated access rules

## Definition of done

Done means:
- existing working pages are still intact
- Connector and Asset Registry still work
- the new pages are visible in the running app
- the new pages use the existing app shell
- navigation shows the new sections
- the new pages do not require redesigning the app

Start with Step 1 only:
Audit the current app and list exactly what already exists and what is missing.
Do not start broad refactoring.
